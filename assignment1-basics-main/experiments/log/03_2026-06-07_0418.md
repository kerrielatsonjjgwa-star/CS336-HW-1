# 监控快照 03 — 2026-06-07 04:18 (+08)

## ⚠️ 用户修正:改用 full run(固定计算预算)
- **每次 run 应处理 327,680,000 token**(batch × steps × ctx)。
- batch=128, ctx=256 → **steps = 327.68M / (128×256) = 10,000 步/run**。
- 之前的 600 步 sweep 远不够,已**作废重来**。

## 本轮动作
1. **杀掉**短 sweep(tmux sweep + 所有 train 进程),清理 600 步产物。
2. **train.py 加 NaN/inf 发散早停**:loss 非有限时打印 `DIVERGED` 并提前返回(iteration + epoch 两模式都加)→ 发散的 full run 不再空跑 42 分钟。
3. **重配 sweep**:`ITERS=10000`(=327.68M token),LR 取 **5 个**(长训练 → 3-5):`1e-4, 3e-4, 6e-4, 1.5e-3, 3e-3`(跨收敛→发散),log/eval 间隔 100/500。
4. **重启** tmux `sweep`,首个 `lr_1e-4` 已开跑。

## 监控读数
| 指标 | 值 |
|---|---|
| GPU 利用率 | **100%**(校准时 39% → 现已吃满) |
| 显存 | 19,512 MiB(batch=128) |
| 当前 run | `lr_1e-4` @ iter 0(loss 9.21) |
| tmux | `sweep` 存活 |

## 预估
- 单 full run ≈ 42 分钟(@4 it/s;若实测更快则更短);收敛的 3 个 ≈ 2 小时,发散的(1.5e-3/3e-3)NaN 早停后几分钟即止。
- **整个 LR sweep 预计 ~2.5–3.5 小时。**
- 后续每 ~25–30 分钟写一份监控快照;全部跑完 → 解析最终 train/val loss(发散标注)→ matplotlib 画学习曲线 → 填 results.md 实验 1 → 启动实验 2。
