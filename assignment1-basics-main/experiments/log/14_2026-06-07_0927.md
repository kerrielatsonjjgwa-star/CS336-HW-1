# 监控快照 14 — 2026-06-07 09:27 (+08)

## 实验 2 收官(batch sweep,同 327.68M token 预算,lr=1.5e-3)
| batch | 步数 | 最终 val loss | 显存 |
|---|---|---|---|
| **64** | 20000 | **1.3472** ⭐ | 10.0 GB |
| 128 | 10000 | 1.3749 | 19.5 GB |
| 192 | 6666 | 1.3837 | 28.9 GB |
| 256 | 5000 | **OOM** ❌ | >32 GB |

**结论**:同 FLOPs 下 **batch 越小损失越低**(更新次数多);最大可行 batch≈192,256 OOM。图已出(exp2_batch_sweep / exp2_batch_final)。

## 🔧 根因修复:所有 pull 失败的真凶
- `.gitignore` 里 `experiments/runs/`、`experiments/plots/` **带内部斜杠 → 被锚定到仓库根,根本没匹配嵌套路径** `assignment1-basics-main/experiments/...`。
- 所以 `git add -A` 一直把 plots 重新跟踪 → box 的未跟踪图与提交冲突 → pull 反复失败。
- 已改为完整路径 `assignment1-basics-main/experiments/{runs,plots}/` + 真正 `git rm --cached`。**pull 不会再失败。**

## 实验 3 启动:epoch vs iteration(同 FLOPs)
- `--max-steps` 生效:`[epoch 模式] 总步数=10000(--max-steps 上限)` ✓。
- epoch 模式(iter_epoch_batches 无放回)跑 10000 步=327.68M token,batch128/lr1.5e-3。
- iteration 结果复用 exp1 的 lr_1.5e-3(val **1.375**)。

## 下一步
- exp3(~34min,~10:00 完成)→ 对比两种加载策略 val loss → 画对比图 → exp3 收尾 → **全部 4 个实验完成,写总结**。

## 监控读数
| 指标 | 值 | 时间 |
|---|---|---|
| GPU | 跑 exp3 epoch 模式 | 09:27 |
