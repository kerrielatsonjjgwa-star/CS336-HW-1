# 监控快照 02 — 2026-06-07 04:10 (+08)

## 校准结果(tmux: calib,已结束 EXIT=0)
- 300 步 / 98s → **≈4 it/s(~250ms/step)@ batch=128**,吞吐 ≈131k tok/s。
- GPU 利用率仅 39%(显存 19.5GB)→ **数据加载瓶颈**(NFS memmap)。
- loss:9.21 → val 3.69,正常下降,无发散。

## 本轮动作:启动实验 1(LR 随机搜索 sweep)
- 驱动脚本 `experiments/sweep_lr.sh`,在 tmux 会话 `sweep` 中顺序跑:
- **8 个学习率**(log 均匀,跨收敛→发散):`3e-5, 1e-4, 3e-4, 6e-4, 1e-3, 2e-3, 4e-3, 8e-3`
- 每个 600 步,batch=128,其余固定;各 run 日志 `runs/lr_<lr>.log`,总进度 `runs/sweep_lr.log`。
- 预计总耗时 ≈ 8 × 3min ≈ **24 分钟**。
- wandb:同 project `tinystories-sweep`,各 run 名 `lr_<lr>`。

## 预期
- 低 LR(3e-5)收敛慢;中段(3e-4~1e-3)最优;高 LR(4e-3/8e-3)可能发散(loss→nan)。
- 下一轮(~25min 后):sweep 应跑完 → 解析各 run 最终 train/val loss(发散标注)→ 装 matplotlib 画学习曲线 → 填 results.md 实验 1。

## 监控读数(本轮启动时)
| 指标 | 值 |
|---|---|
| GPU | 0%(校准结束,sweep 即将开跑) |
| 残留进程 | 无 |
