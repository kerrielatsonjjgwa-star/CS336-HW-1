# 监控快照 11 — 2026-06-07 08:05 (+08)

## 实验 1 完全收尾 ✅(7 个 LR + 发散结论)

| LR | val loss | |
|---|---|---|
| 1e-4 | 1.9491 | |
| 3e-4 | 1.5500 | |
| 6e-4 | 1.4311 | <1.45 |
| **1.5e-3** | **1.3749** | 最优 ⭐ |
| 3e-3 | 1.3782 | |
| 6e-3 | 2.2009 | 软发散(退化) |
| 1.2e-2 | ~3.02(止损) | 更退化 |

**发散结论**:≥6e-3 loss 单调恶化、不收敛;但 **grad_clip=1.0 截断梯度 → 全程无 NaN 硬发散**,发散表现为 loss 退化/停滞。可用 LR 上界 ≈3e-3,最优 1.5e-3。图已更新为 7-LR 英文版。

## 修了一个 git 坑
- `plots/*.png` 之前 `git rm --cached` 没生效 → 一直被当"已修改跟踪文件"挡住 box 的 pull → box 卡旧 commit。已彻底取消跟踪(设为生成物),pull 恢复正常。

## 实验 2 启动:batch sweep
- **batch_256 → OOM** ✗(`CUDA out of memory`,需 ~32GB+;5090 32G 装不下)。
- **batch_192 → 正在跑**(装得下)→ **最大可行 batch ≈ 192**。
- batch_64 排队;batch_128 复用 exp1(val 1.375)。
- 固定 lr=1.5e-3 + 固定 327.68M token 预算(步数随 batch 反比)。

## 监控读数
| 指标 | 值 |
|---|---|
| GPU | 跑 batch_192 |
| tmux | `batch` |
| 时间 | 08:05 |

## 下一步
- batch_192(~22min)→ batch_64(~50min)跑完 → 画 batch 学习曲线 → exp2 收尾 → **实验 3(epoch vs iteration,同 FLOPs)**。
