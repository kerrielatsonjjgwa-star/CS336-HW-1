# 监控快照 15(最终)— 2026-06-07 10:00 (+08)

## 🎉 全部 4 个实验完成,campaign 收官

| 实验 | 结论 | 图 |
|---|---|---|
| 1. LR sweep | 最优 lr≈**1.5e-3**(1.375);≥6e-3 软发散(grad_clip 无 NaN) | exp1_lr_sweep / exp1_final_vs_lr |
| 2. batch sweep | batch 越小损失越低(64→**1.347**);最大≈192,**256 OOM** | exp2_batch_sweep / exp2_batch_final |
| 3. epoch vs iteration | 同 FLOPs **等价**(1.359 vs 1.375,噪声内) | exp3_loader |
| 4. val < 1.45 | **达成**,最低 **1.347** | (见 exp1/exp2 目标线) |

## 最佳配置
d_model=512/4层/16头/d_ff=1344/ctx=256 + **lr=1.5e-3 · batch=64 · 20000步(327.68M token)** → **val/loss 1.347**。

## 生成样例(batch_64,val 1.347)
> Once upon a time, there was a little girl named Lily. She loved to play with her friends outside... Tom played with the cushion, but it was too rough. He broke it in half... Lily and Tom worked together to fix the cushion.

→ 语法/情节/对话完整,TinyStories 风格,**模型训练成功**。

## 产出清单
- `results.md`:4 实验完整报告 + 结论 + 样例。
- `plots/`:exp1(2 图)、exp2(2 图)、exp3(1 图)学习曲线(英文标签)。
- `log/01–15.md`:全程监控快照。
- wandb project `tinystories-sweep` / `tinystories-batch` / `tinystories-loader`:实时曲线存档。
- 各 run checkpoint 在 `runs/`(卡上,gitignored)。

## ⏹️ loop 结束
- 4 实验全部完成,无待跑任务 → **停止 /loop 自定步循环**。
- GPU 现已空闲。**如不再训练,可释放租用的卡停止计费**(checkpoint 在临时盘,需要的话先下载;vocab/merges/.bin 在 shared-nvme 持久盘)。

## 整夜 campaign 用时
04:00 启动 → 10:00 收官,~6 小时:数据(已就绪)→ 4 实验(约 14 个 full run + OOM 探测)全自动完成 + 监控 + 出图。
