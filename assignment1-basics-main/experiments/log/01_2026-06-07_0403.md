# 监控快照 01 — 2026-06-07 04:03 (+08)

## 本轮动作
- 搭好实验框架:`experiments/results.md`(报告)、`run_train.sh`(通用训练封装)、`log/`(监控快照)、`runs/`(训练日志/ckpt,gitignored)。
- 卡上装好 tmux 3.4;GPU 空闲(1MiB)→ 现已启动**校准训练**。

## 校准训练(tmux: calib)
- 配置:batch=128, lr=3e-4, max-iters=300, ctx=256, 17M 模型。
- 目的:测 batch=128 的 it/s,据此决定各实验的 per-run 步数与 sweep 次数。
- wandb 实盘:https://wandb.ai/kerrielatsonjjgwa-tsinghua-university/tinystories-sweep (run `calib`)。

## 监控读数
| 指标 | 值 |
|---|---|
| GPU 利用率 | 39% |
| 显存占用 | 19,512 MiB / 32,607 MiB(batch=128) |
| tmux 会话 | `calib` 存活 |
| 训练步日志 | 暂未刷出(见下「问题」) |

## 发现 / 问题
1. **stdout 块缓冲**:`print` 输出经管道 tee 时被 Python 块缓冲,iter/loss 未实时落盘。
   → 已给 `run_train.sh` 加 `PYTHONUNBUFFERED=1`,后续 run 实时可见;校准本次按 START/END 时间戳算速度。
2. **显存预估**:batch=128 占 19.5GB(注意力 batch×heads×ctx² 是大头)→ 实验 2 中 batch=256 预计 OOM,最大可行 batch 估 ~192。

## 下一步
- 等校准跑完 → 读 it/s → 配置并启动**实验 1(LR 随机搜索 sweep)**驱动脚本(多次顺序训练)。
